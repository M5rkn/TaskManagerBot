# Task Manager Bot

Многофункциональный бот для управления задачами с интеграцией Google Calendar и напоминаниями.

## 🚀 Возможности

- ✅ Создание, редактирование и удаление задач
- 📅 Интеграция с Google Calendar
- 🔔 Напоминания о дедлайнах
- 🎯 Приоритеты задач (низкий, средний, высокий)
- 📊 Статистика выполнения задач
- ⏰ Проверка просроченных задач

## 🛠️ Технологии

- **Python 3.11+**
- **Aiogram 3.x** - фреймворк для Telegram ботов
- **SQLite** - база данных
- **Redis** - очередь напоминаний
- **Google Calendar API** - синхронизация с календарем

## 📦 Установка

### 1. Перейдите в директорию проекта

```bash
cd "C:\Users\Марк\Desktop\TaskManager"
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
```

### 3. Активируйте виртуальное окружение

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### 4. Установите зависимости

```bash
python -m pip install -r requirements.txt
```

### 5. Настройте переменные окружения

```bash
cp .env.example .env
```

Отредактируйте `.env` файл:

```env
# Telegram Bot (получите у @BotFather)
BOT_TOKEN=your_bot_token_here

# Google Calendar API (опционально)
GOOGLE_CREDENTIALS_FILE=credentials.json
GOOGLE_CALENDAR_ID=primary

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# SQLite Database
DATABASE_PATH=tasks.db

# Timezone
TIMEZONE=Europe/Kiev
```

### 6. Настройте Google Calendar (опционально)

1. Скачайте `credentials.json` от вашего Google Cloud Service Account
2. Поделитесь календарем с email сервисного аккаунта

## 🚀 Запуск

```bash
python main.py
```

## 📱 Команды бота

| Команда | Описание |
|---------|------|
| `/start` | Запуск бота |
| `/help` | Показать справку |

## 📂 Структура проекта

```
TaskManager/
├── bot/                    # Файлы бота
│   ├── main.py            # Главная точка входа
│   ├── states.py          # FSM состояния
│   └── keyboards.py       # Inline клавиатуры
├── config/                 # Конфигурация
│   └── settings.py        # Настройки
├── db/                     # База данных
│   ├── database.py        # Подключение к SQLite
│   └── repositories.py    # Репозитории
├── handlers/               # Хендлеры бота
│   ├── commands.py        # Команды
│   ├── tasks.py           # Управление задачами
│   └── cancel.py          # Отмена
├── models/                 # Модели данных
│   └── task.py            # Модели Task, User, Reminder
├── services/               # Сервисы
│   ├── google_calendar.py # Google Calendar API
│   └── reminder_service.py # Redis напоминания
├── .env.example           # Пример переменных окружения
├── requirements.txt       # Зависимости
└── main.py               # Точка запуска
```

## 🔧 Разработка

### Добавление новых хендлеров

1. Создайте новый файл в `handlers/`
2. Добавьте роутер: `router = Router()`
3. Импортируйте в `bot/main.py`

### Миграции базы данных

Для изменений в структуре БД редактируйте `db/database.py` и обновите функцию `create_tables()`.

## 📝 Лицензия

MIT

## 👨‍💻 Автор

Task Manager Bot
